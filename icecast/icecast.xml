<icecast>
    <!-- location and admin are two arbitrary strings that are e.g. visible
         on the server info page of the icecast web interface
         (server_version.xsl). -->
    <location>Location</location>
    <admin>email@example.com</admin>

    <!-- This is the hostname other people will use to connect to your server.
         It affects mainly the urls generated by Icecast for playlists and yp
         listings. You MUST configure it properly for YP listings to work!
    -->
    <hostname>your.hostname.com</hostname>

    <limits>
        <clients>100</clients>
        <sources>2</sources>
        <queue-size>524288</queue-size>
        <client-timeout>30</client-timeout>
        <header-timeout>15</header-timeout>
        <source-timeout>10</source-timeout>
        <burst-on-connect>1</burst-on-connect>
        <burst-size>65535</burst-size>
    </limits>

    <authentication>
		<!-- Admin logs in with the username given below -->
        <admin-user>admin</admin-user>
        <admin-password>adminpassword</admin-password>
		<!-- Sources log in with username 'source' -->
		<source-password>sourcepassword</source-password>
		<!-- Relays log in with username 'relay' -->
        <relay-password>relaypassword</relay-password>
    </authentication>

    <!-- Uncomment this if you want directory listings -->
    <!-- <directory>
        <yp-url-timeout>15</yp-url-timeout>
        <yp-url>http://dir.xiph.org/cgi-bin/yp-cgi</yp-url>
    </directory> -->

    <!-- You may have multiple <listener> elements -->
    <listen-socket>
        <port>10253</port>
    </listen-socket>

    <!-- <listen-socket>
        <port>10453</port>
        <ssl>1</ssl>
    </listen-socket> -->

    <!-- <http-headers>
        <header name="Access-Control-Allow-Origin" value="https://localhost" />
    </http-headers> -->
	
	<mount>
		<mount-name>/live</mount-name>
		<password>mlivepassword</password>
		<max-listeners>500</max-listeners>
		<public>1</public>
		<burst-size>65536</burst-size>
		<mp3-metadata-interval>8192</mp3-metadata-interval>	   
		<charset>utf-8</charset>
		<fallback-mount>/nonstop</fallback-mount>
        <fallback-override>1</fallback-override>
	</mount>
	
	<mount>
		<mount-name>/nonstop</mount-name>
		<password>mnonstoppassword</password>
		<max-listeners>500</max-listeners>
		<public>1</public>
		<burst-size>65536</burst-size>
		<mp3-metadata-interval>8192</mp3-metadata-interval>	   
		<charset>utf-8</charset>
	</mount> 


    <fileserve>1</fileserve>

    <paths>
        <logdir>/var/log/icecast</logdir>
        <webroot>/usr/share/icecast/web</webroot>
        <adminroot>/usr/share/icecast/admin</adminroot>

        <!-- Aliases: treat requests for 'source' path as being for 'dest' path
             May be made specific to a port or bound address using the "port"
             and "bind-address" attributes.
          -->
        <!--
        <alias source="/foo" destination="/bar"/>
        -->
        <!-- Aliases: can also be used for simple redirections as well,
             this example will redirect all requests for http://server:port/ to
             the status page
        -->
        <alias source="/" destination="/status.xsl"/>
        <!-- The certificate file needs to contain both public and private part.
             Both should be PEM encoded.
        -->
		<!-- <ssl-certificate>/path/to/sertificate</ssl-certificate> -->
    </paths>

    <logging>
        <accesslog>access.log</accesslog>
        <errorlog>error.log</errorlog>
        <loglevel>3</loglevel> <!-- 4 Debug, 3 Info, 2 Warn, 1 Error -->
        <logsize>10000</logsize> <!-- Max size of a logfile -->
    </logging>
	
	<security>
        <chroot>0</chroot>
        <changeowner>icecast:icecast</changeowner> 
	</security>
</icecast>
